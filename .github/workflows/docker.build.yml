name: Build & Push Docker (RunPod)

on:
  push:
    branches: [ main ]
    paths:
      - 'Dockerfile'
      - 'scripts/**'
      - 'bin/**'
      - '.github/workflows/**'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag additionnel (ex: v1.0.0)'
        required: false
        default: ''

env:
  IMAGE_REPO: docker.io/mauricebourdondock/runpod-comfyui-c128-wan22-v1

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Compute tags (no latest)
        id: meta
        run: |
          SHA="${GITHUB_SHA::7}"
          EXTRA="${{ github.event.inputs.tag }}"
          # Tags sûrs : un tag stable RunPod + un tag immuable par commit
          TAGS="${IMAGE_REPO}:runpod-amd64-v1,${IMAGE_REPO}:${SHA}"
          if [ -n "$EXTRA" ]; then TAGS="${TAGS},${IMAGE_REPO}:${EXTRA}"; fi
          echo "tags=${TAGS}" >> $GITHUB_OUTPUT

      - name: Build & Push (linux/amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=${{ env.IMAGE_REPO }}:buildcache
          cache-to: type=registry,ref=${{ env.IMAGE_REPO }}:buildcache,mode=max
